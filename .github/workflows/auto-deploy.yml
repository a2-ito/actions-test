name: Automatic deployment
on:
  push:
    branches:
      - dev
      - stg
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.pull_request.merged == true }}

    steps:
      - name: check
        run: |
          echo ${{ github.ref }}" is merged!"
